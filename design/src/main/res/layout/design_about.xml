<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="versionName"
            type="String" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/dialog_padding">

        <!-- Header: icon + name -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <View
                android:layout_width="@dimen/about_icon_size"
                android:layout_height="@dimen/about_icon_size"
                android:background="@drawable/ic_flowly_logo" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="@dimen/about_text_margin"
                android:orientation="vertical">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/application_name"
                    android:textAppearance="@style/TextAppearance.MaterialComponents.Body1"
                    android:textStyle="bold" />

                <TextView
                    android:id="@+id/about_version_text"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:text='@{"v" + versionName}'
                    android:textAppearance="@style/TextAppearance.MaterialComponents.Caption"
                    android:alpha="0.7"
                    android:fontFamily="monospace" />
            </LinearLayout>
        </LinearLayout>

        <!-- Tagline -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="@string/about_tagline"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            android:alpha="0.6" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="16dp"
            android:background="?colorLineSoft" />

        <!-- Info rows -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="@string/about_info_core"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            android:alpha="0.5"
            android:lineSpacingExtra="4dp" />

        <!-- GitHub link – 居中蓝色可点按钮样式 -->
        <TextView
            android:id="@+id/about_github_row"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:background="@android:drawable/edit_text"
            android:backgroundTint="?attr/colorSurface"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:paddingTop="10dp"
            android:paddingBottom="10dp"
            android:text="@string/about_github_link_text"
            android:textColor="?colorPrimary"
            android:textSize="14sp" />

        <!-- Kernel / mihomo link – 同上按钮样式 -->
        <TextView
            android:id="@+id/about_kernel_row"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@android:drawable/edit_text"
            android:backgroundTint="?attr/colorSurface"
            android:clickable="true"
            android:focusable="true"
            android:gravity="center"
            android:paddingTop="10dp"
            android:paddingBottom="10dp"
            android:text="@string/about_kernel_link_text"
            android:textColor="?colorPrimary"
            android:textSize="14sp" />
    </LinearLayout>
</layout>